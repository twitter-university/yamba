Use a ContentResolver notification, for updates on ContentProvider access to DB

getting keys from prefs should use the resource

Toast gets dropped if there is no activity

==================  Done

twitter object in StatusActivity must be volatile

Text count is wrong after rotate

titlePrefs > prefsTitle in ex06

use sasa's library, not jtwitter

Add to Manifest:
        <!-- 
        android:uiOptions="splitActionBarWhenNarrow" 
        android:theme="@android:style/Theme.Holo.NoActionBar"
         -->

Move menu management to application?

Create the listAdaptor before initializing the loader manager

Add the SafeTwitter client

ViewBinder should be, explicitly, the SimpleCursorAdapter.ViewBinder

use TABLE_TIMELINE, not TABLE

Be sure that Yamba Client has a different name
